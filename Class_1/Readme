吴恩达机器学习第一课课后作业
主要利用代码实现单变量线性回归以及多变量线性回归
有几个重要概念如下：

1. 代价函数的计算
线性回归的代价函数为：J(θ) = 1/2m Σ(h(x) - y)^2^
其中 h(x) = θTx

2. 梯度下降
θ ：= θ-α/m Σ ((h(x) - y))x

这里要注意矩阵的维度，代码中分步写出来了

第一次使用github，还没学会如何正确编辑公式，争取在第二篇上有所改进
